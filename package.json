{
  "name": "wp-rocket-scripts",
  "version": "1.0.2",
  "description": "Rocket main scripts packages",
  "type": "module",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "mocha test",
    "build:unmin": "esbuild ./src/rocketLcpBeacon.js --bundle --outfile=dist/lcp-beacon.js",
    "build:min": "esbuild ./dist/lcp-beacon.js --allow-overwrite --minify --sourcemap --outfile=dist/lcp-beacon.min.js",
    "build": "npm run build:unmin && npm run build:min"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wp-media/rocket-scripts.git"
  },
  "author": "WP Media Engineering Team",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/wp-media/rocket-scripts/issues"
  },
  "homepage": "https://github.com/wp-media/rocket-scripts#readme",
  "devDependencies": {
    "esbuild": "0.23.0",
    "mocha": "^10.6.0",
    "node-fetch": "^3.3.2",
    "sinon": "^18.0.0"
  }
}
